import { useEffect, useMemo, useState } from "react";
import { supabase } from "../lib/supabase";
import { useBranch } from "../context/BranchContext";

type Row={id:string;amount:number;reason:string;createdAt:string;status:string;employeeName:string};
export default function Withdrawals(){const {effectiveBranchId}=useBranch();const [rows,setRows]=useState<Row[]>([]),[amount,setAmount]=useState(""),[reason,setReason]=useState(""),[busy,setBusy]=useState(false);useEffect(()=>{void load()},[effectiveBranchId]);async function load(){let q=supabase.from("employee_withdrawals").select("id,amount,reason,status,created_at,user_id,user_profiles:user_id(full_name,username)").order("created_at",{ascending:false});if(effectiveBranchId)q=q.eq("branch_id",effectiveBranchId);const {data,error}=await q;if(error)return alert(error.message);setRows((data||[]).map((x:any)=>{const p=Array.isArray(x.user_profiles)?x.user_profiles[0]:x.user_profiles;return{id:String(x.id),amount:Number(x.amount),reason:String(x.reason||""),createdAt:String(x.created_at),status:String(x.status),employeeName:String(p?.full_name||p?.username||"موظف")}}))}async function save(){const value=Number(amount);if(!(value>0))return alert("اكتب مبلغ صحيح");setBusy(true);const {data:{user}}=await supabase.auth.getUser();const {error}=await supabase.from("employee_withdrawals").insert({user_id:user?.id||null,branch_id:effectiveBranchId||null,amount:value,reason:reason.trim()||null,status:"approved"});setBusy(false);if(error)return alert(error.message);setAmount("");setReason("");void load()}const total=useMemo(()=>rows.filter(x=>x.status==="approved").reduce((s,x)=>s+x.amount,0),[rows]);return <div dir="rtl" className="space-y-5 p-3 sm:p-6 lg:p-8"><header><h1 className="text-2xl font-black sm:text-3xl">مسحوبات الموظفين</h1><p className="mt-1 text-sm text-gray-500">كل مسحوب يُسجل تلقائيًا باسم المستخدم الحالي ويظهر في ملف الموظف.</p></header><div className="rounded-2xl bg-emerald-50 p-4 font-black text-emerald-800">إجمالي المسحوبات المعتمدة: {total.toFixed(2)} د.ل</div><section className="rounded-2xl bg-white p-4 shadow-sm"><div className="grid gap-3 sm:grid-cols-2"><input type="number" className="rounded-xl border p-3" placeholder="المبلغ" value={amount} onChange={e=>setAmount(e.target.value)}/><input className="rounded-xl border p-3" placeholder="سبب المسحوب" value={reason} onChange={e=>setReason(e.target.value)}/></div><button disabled={busy} onClick={()=>void save()} className="mt-3 w-full rounded-xl bg-emerald-700 p-3 font-bold text-white sm:w-auto">تسجيل المسحوب</button></section><section className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{rows.map(x=><article key={x.id} className="rounded-2xl bg-white p-4 shadow-sm"><div className="flex justify-between gap-3"><strong>{x.employeeName}</strong><strong className="text-red-700">{x.amount.toFixed(2)} د.ل</strong></div><p className="mt-2 text-sm text-gray-600">{x.reason||"بدون سبب"}</p><p className="mt-2 text-xs text-gray-400">{new Date(x.createdAt).toLocaleString("ar-LY")}</p></article>)}</section></div>}
