import { useEffect, useMemo, useState } from "react";
import { supabase } from "../lib/supabase";
import { useBranch } from "../context/BranchContext";

type Employee={id:string;name:string;role:string;phone:string;active:boolean;withdrawals:number;absences:number};
export default function Employees(){const {effectiveBranchId}=useBranch();const [rows,setRows]=useState<Employee[]>([]),[loading,setLoading]=useState(true),[search,setSearch]=useState("");useEffect(()=>{void load()},[effectiveBranchId]);async function load(){setLoading(true);try{let q=supabase.from("user_profiles").select("id,full_name,username,phone,is_active,branch_id,role_id").order("full_name");if(effectiveBranchId)q=q.eq("branch_id",effectiveBranchId);const {data:profiles,error}=await q;if(error)throw error;const ids=(profiles||[]).map((x:any)=>String(x.id));const roleIds=[...new Set((profiles||[]).map((x:any)=>x.role_id).filter(Boolean))];const [rolesResult,wResult,aResult]=await Promise.all([roleIds.length?supabase.from("roles").select("id,name").in("id",roleIds):Promise.resolve({data:[],error:null}),ids.length?supabase.from("employee_withdrawals").select("user_id,amount,status").in("user_id",ids):Promise.resolve({data:[],error:null}),ids.length?supabase.from("employee_absences").select("user_id,status").in("user_id",ids):Promise.resolve({data:[],error:null})]);if(rolesResult.error)console.warn(rolesResult.error.message);if(wResult.error)console.warn(wResult.error.message);if(aResult.error)console.warn(aResult.error.message);const rm=new Map((rolesResult.data||[]).map((r:any)=>[String(r.id),String(r.name||"employee")]));const wm=new Map<string,number>(),am=new Map<string,number>();(wResult.data||[]).filter((x:any)=>x.status==="approved").forEach((x:any)=>wm.set(String(x.user_id),(wm.get(String(x.user_id))||0)+Number(x.amount||0)));(aResult.data||[]).filter((x:any)=>x.status==="absent").forEach((x:any)=>am.set(String(x.user_id),(am.get(String(x.user_id))||0)+1));setRows((profiles||[]).map((x:any)=>({id:String(x.id),name:String(x.full_name||x.username||"موظف"),role:rm.get(String(x.role_id))||"employee",phone:String(x.phone||""),active:Boolean(x.is_active),withdrawals:wm.get(String(x.id))||0,absences:am.get(String(x.id))||0})));}catch(error){alert(error instanceof Error?error.message:"تعذر تحميل الموظفين");}finally{setLoading(false)}}const filtered=useMemo(()=>rows.filter(x=>`${x.name} ${x.role} ${x.phone}`.toLowerCase().includes(search.toLowerCase())),[rows,search]);const total=rows.reduce((s,x)=>s+x.withdrawals,0);return <div dir="rtl" className="space-y-5 p-3 sm:p-6 lg:p-8"><header><h1 className="text-2xl font-black sm:text-3xl">الموظفون</h1><p className="mt-1 text-sm text-gray-500">ملخص الموظف حسب الفرع: الحالة، الغياب، والمسحوبات.</p></header><div className="grid grid-cols-2 gap-3 lg:grid-cols-4"><Card title="الموظفون" value={rows.length}/><Card title="النشطون" value={rows.filter(x=>x.active).length}/><Card title="الغياب المسجل" value={rows.reduce((s,x)=>s+x.absences,0)}/><Card title="إجمالي المسحوبات" value={`${total.toFixed(2)} د.ل`}/></div><section className="rounded-2xl bg-white p-4 shadow-sm"><input className="w-full rounded-xl border p-3" placeholder="بحث بالاسم أو الوظيفة أو الهاتف" value={search} onChange={e=>setSearch(e.target.value)}/></section>{loading?<div className="p-8 text-center">جاري التحميل...</div>:<section className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{filtered.map(x=><article key={x.id} className="rounded-2xl bg-white p-4 shadow-sm"><div className="flex items-start justify-between"><div><h3 className="text-lg font-black">{x.name}</h3><p className="text-sm text-gray-500">{x.role} {x.phone?`— ${x.phone}`:""}</p></div><span className={`rounded-full px-3 py-1 text-xs font-bold ${x.active?"bg-green-100 text-green-700":"bg-red-100 text-red-700"}`}>{x.active?"نشط":"موقوف"}</span></div><div className="mt-4 grid grid-cols-2 gap-3"><div className="rounded-xl bg-red-50 p-3"><p className="text-xs text-gray-500">الغياب</p><strong className="text-red-700">{x.absences} يوم</strong></div><div className="rounded-xl bg-amber-50 p-3"><p className="text-xs text-gray-500">المسحوبات</p><strong className="text-amber-700">{x.withdrawals.toFixed(2)} د.ل</strong></div></div></article>)}</section>}</div>}
function Card({title,value}:{title:string;value:string|number}){return <div className="rounded-2xl bg-white p-4 shadow-sm"><p className="text-xs text-gray-500 sm:text-sm">{title}</p><p className="mt-2 text-xl font-black text-emerald-700 sm:text-2xl">{value}</p></div>}
